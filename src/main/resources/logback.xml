<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    
    <!-- Console: только WARN и выше (ошибки) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>WARN</level>
        </filter>
        <encoder>
            <pattern>⚠️  %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- File: INFO и выше (не DEBUG в production) -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>mz4d.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>mz4d.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Project loggers - INFO by default, DEBUG via system property -->
    <logger name="io.github.manjago.mz4d" level="${mz4d.log.level:-INFO}" additivity="false">
        <appender-ref ref="FILE" />
        <appender-ref ref="CONSOLE" />
    </logger>
    
    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="FILE" />
    </root>
    
</configuration>
